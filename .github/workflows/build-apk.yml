name: Build MiniFlip OS APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install required Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Restore MiniFlip debug signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: miniflip-debug-keystore-v1

      - name: Apply Irving OS v0.16 interaction patch
        run: python3 scripts/apply_v016.py

      - name: Apply Irving OS v0.17 cover app launch patch
        run: python3 scripts/apply_v017.py

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - uses: actions/upload-artifact@v4
        with:
          name: Irving-OS-v0.18-phone-background-fix
          path: app/build/outputs/apk/debug/app-debug.apk
